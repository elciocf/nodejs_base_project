class {{entity}} {
    {{#each fields}}
    {{@key}}: {{this}};
    {{/each}}

  constructor() {
        if (!this.{{entity_pk}}) {
            this.created_at = new Date();
            this.updated_at = new Date();
        }
   }
}

export { {{entity}} };


