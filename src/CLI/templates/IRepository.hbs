import { I{{entity}}DTO } from "@modules/{{module}}/dtos/I{{entity}}DTO";
import { {{entity}} } from "@modules/{{module}}/entities/{{entity}}";

interface IList {
    data: {{entity}}[];
    count: number;
    limit: number;
    page: number;
    totalPages: number;
}

interface I{{entityPlural}}Repository {
    create(data: I{{entity}}DTO): Promise<{{entity}}>;
    update(data: I{{entity}}DTO): Promise<{{entity}}>;
    delete({{entity_pk}}: number): Promise<void>;
    getByPK({{entity_pk}}: number): Promise<{{entity}}>;
    listAll(page: number, limit?: number, order?: string): Promise<IList>;
}

export { I{{entityPlural}}Repository, IList };
