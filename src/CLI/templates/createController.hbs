import { Response, Request } from "express";
import { container } from "tsyringe";

import { {{useCaseName}}UseCase } from "./{{useCaseName}}UseCase";

class {{useCaseName}}Controller {
    async handle(request: Request, response: Response): Promise<Response> {
        const data = request.body;

        const {{useCaseNameLowerFirst}}UseCase = container.resolve({{useCaseName}}UseCase);

        const {{entityLower}} = await {{useCaseNameLowerFirst}}UseCase.execute(data);
        return response.status(201).json({{entityLower}});
    }
}

export { {{useCaseName}}Controller };